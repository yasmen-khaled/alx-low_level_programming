#include "main.h"

/**
 * _isupper - check if c is upper
 *
 * @c: input for alphbet
 *
 * Return: 1 if its uppercase, 0 
 */

int _isupper(int n)
{
if (n >= 65 && n <= 90)
return (1);
else
return (0);
}
