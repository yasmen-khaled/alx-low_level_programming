#include "main.h"

/**
 * _isupper - check if c is upper
 *
 * @c: input for alphbet
 *
 * Return: 1 if its uppercase, 0 if not
 */

int _isupper(int k)
{
if (k >= 65 && k <= 90)
return (1);
else
return (0);
}
