#include "main.h"

/**
 * _isdigit - check if input is digit between 0 - 9
 *
 * @j: input
 *
 * Return: 1 if its uppercase, 0 if not
 */

int _isdigit(int j)
{
if (j >= 48 && j <= 57)
return (1);
else
return (0);
}

