#include "main.h"

/**
 * _strlen - returns the lenght of a string
 *
 * @s: string
 *
 * Return: length
 */

int _strlen(char *s)
{
int length = 0;
while (*ptr != '\0')
{
length++;
ptr++;
}
return (length);
}
